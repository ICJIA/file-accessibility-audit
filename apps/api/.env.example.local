NODE_ENV=development
PORT=5103

# Database
DB_PATH=./data/audit.db

# Limits
MAX_FILE_SIZE_MB=15
TMP_DIR=/tmp

# Optional: fleet-integration service token (rate-tier + URL-allowlist bypass).
# Not a user credential — the tool has no accounts or sign-in.
# API_PRIVILEGED_TOKEN=
